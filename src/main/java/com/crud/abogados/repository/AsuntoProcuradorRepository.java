package com.crud.abogados.repository;

import com.crud.abogados.entity.AsuntoProcurador;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AsuntoProcuradorRepository extends JpaRepository<AsuntoProcurador, Long> {
}
